# Public — safe to expose to the browser (Supabase's anon key is designed for this)
NEXT_PUBLIC_SUPABASE_URL=https://fglsfkjxdlarqjyhqmvt.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_Hw3ToLJc1vhnGwLoJz67nw_lIuLwLs7

# Server-only — used in Server Actions / Route Handlers, never sent to the browser.
# Pull this from Supabase dashboard → Project Settings → API → service_role key.
# Needed if any Edge Function calls (generate-gap-analysis, send-email) move server-side
# and require elevated permissions beyond what RLS allows the anon key.
SUPABASE_SERVICE_ROLE_KEY=
